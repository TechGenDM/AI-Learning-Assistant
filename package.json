{
  "name": "ai-learning-assistant-root",
  "version": "1.0.0",
  "description": "Root package.json for monorepo deployments",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend/ai-learning-assistant",
    "build": "npm run build --prefix frontend/ai-learning-assistant",
    "start": "npm start --prefix backend",
    "dev": "npm run dev --prefix backend & npm run dev --prefix frontend/ai-learning-assistant"
  },
  "engines": {
    "node": ">=18"
  }
}
